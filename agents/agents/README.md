# BRI Agents – Home Assistant Demo

This folder is reserved for scripts and artifacts used by the Build, Release, and Integration (BRI) “agent layer”.

Planned contents:
- `analyze_bri_run.py` – parse build/test/release/policy logs and emit recommendations.
- `last_run_summary.md` – short human-readable summary of the latest CI run.
